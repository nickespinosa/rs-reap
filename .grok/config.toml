# Project-scoped Grok Build config.
# Instructions: AGENTS.md
# rules/skills → .agents/{rules,skills}
# agents → .claude/agents (which @-import .agents/prompts)

[permission]
# Runtime approvals; shared rules via .grok/rules symlink.

[compat.claude]
skills = true
rules = true
agents = true
hooks = true

[compat.cursor]
skills = false
rules = false
agents = false
